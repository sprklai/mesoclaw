/** Minimum character length for provider IDs. */
export const PROVIDER_ID_MIN = 3;
/** Maximum character length for provider IDs. */
export const PROVIDER_ID_MAX = 50;
/** Valid provider ID characters: lowercase alphanumeric and hyphens only. */
export const PROVIDER_ID_PATTERN = /^[a-z0-9-]+$/;
